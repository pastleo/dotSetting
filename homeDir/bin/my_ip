#!/bin/bash

for i in eth0 en0; do
    if ifconfig $i &> /dev/null; then
        MY_IP=$(/sbin/ifconfig $i | awk '/inet/ { print $2 } ' | sed -e s/addr://)
        break
    fi
done

echo ${MY_IP:-"Not connected or not found"}
